# 小确闲开发进度交接

> 更新时间：2026-08-24
> 当前 GitHub 分支：`main`  
> 本次交接基线：`555da69 feat: add moderation decision history`

本文用于下次继续开发、部署或测试时快速恢复上下文。详细接口、迁移和生产规划分别见：

- `小确闲_后端开发文档.md`
- `前端界面接口文档.md`
- `小确闲_生产环境部署规划.md`

---

## 1. 当前结论

核心用户业务后端代码已完成至 M7，M8 审核后台也已实现但暂不作为当前上线门禁。按照独立开发者目标，当前重心是：**M1–M7 云端部署确认、真实账号验收和可回归的稳定发布基线**。M8/P8 先保留代码，等上线后确有举报运营需求再启用。

已完成的代码不等于已部署到 Supabase。本项目后续沟通、测试和发布必须区分以下三种状态：

| 状态 | 含义 |
|---|---|
| 代码完成 | 已提交至 GitHub，并通过项目静态/后端契约检查 |
| 云端已部署 | 数据库迁移和对应 Edge Function 已推送至当前 Supabase 项目 |
| 已验收 | 使用专门测试账号完成真实接口和跨页面验证 |

当前仓库的自动检查只验证代码结构和高风险链路，**不连接 Supabase、不部署数据库、不读取或保存密钥**。

---

## 2. 最新提交

| 提交 | 内容 |
|---|---|
| `555da69` | M8 审核处理历史：审核员可查看去身份化的内容、决定和时间 |
| `21e8583` | 具有审核角色的账号登录首页后自动进入 P7 审核后台 |
| `9f4e766` | 新增独立 P7 审核后台页面，不加入普通用户导航 |
| `efec744` | M8 审核角色、举报队列、审核决定和审计记录后端 |
| `bcfb71e` | GitHub Actions 自动运行静态与后端契约检查 |
| `489c9de` | 增加后端契约检查基础 |

---

## 3. 已完成模块

| 模块 | 代码状态 | 主要能力 |
|---|---|---|
| M0 | 完成 | Supabase 工程、安全基础、RLS 默认收紧、统一错误约定 |
| M1 | 完成 | 注册、登录、恢复码、找回密码、改密、账号注销 |
| M2 | 完成 | 个人档案、积分余额与流水、签到读取 |
| M3 | 完成 | 领导外观、动作、倒地、复活、倒地后重新捏领导 |
| M4 | 完成 | 匿名发声、随机墙、共鸣、奖励领取、举报与三人自动隐藏 |
| M5 | 完成 | 已发布匿名内容的释怀、销毁、24 小时奖励与幂等结算 |
| M6 | 完成 | 签到、关怀结算、下班/喝水/久坐偏好同步 |
| M7 | 代码完成 | 账号硬删除、15 天公开内容过滤与物理清理、契约检查、CI |
| M8 | 暂缓启用 | 受控人工审核、审核决定审计、审核处理历史、P7/P8；代码保留，不进入当前核心上线门禁 |

## 3.1 按独立开发者目标的真实进度

以下比例只针对“先开发完成并稳定上线”的当前目标，不把未来运营后台重复计入：

| 阶段 | 进度 | 说明 |
|---|---:|---|
| 核心功能代码（M1–M7） | 约 95% | 主要接口、迁移、幂等、错误处理和前端接入已存在 |
| 代码质量检查 | 约 95% | 静态检查、后端契约检查、关键脚本语法检查已建立 |
| Supabase 云端部署 | 未确认，按 0–30% 计 | 当前工作区没有完整的远端迁移/Function 部署证据，不能把代码提交当成云端完成 |
| 核心真实验收 | 约 20% | 已确认部分登录/权限链路，M1–M7 全链路尚未逐模块留存验收结果 |
| 独立开发者可上线基线 | 约 65% | 主要差距是部署、真实数据联调、回归和定时任务配置 |

因此不能说“已经稳定上线”，更准确的结论是：**核心业务开发接近完成，项目处于云端部署和验收阶段**。

---

## 4. 审核后台现状（当前暂缓）

### 已有功能

- P7 页面：`P7_审核后台.html`；普通用户导航没有入口。
- 审核账号在 P1 输入账号和密码登录成功后，由后端角色校验决定是否自动跳转 P7。
- 角色为 `moderator` / `admin` 的用户可读取举报队列并做出：`approved`、`hidden`、`rejected` 决定。
- 每次决定带 `request_id`，重复提交不会重复产生审核记录。
- P7 可显示最近审核处理摘要；不展示发布者、举报者、审核员身份或内部备注。
- P8 页面：`P8_角色管理.html`；仅管理员可用，支持授予/撤销 `moderator`，普通用户和 moderator 由服务端拒绝。

M8/P8 当前不阻塞核心上线。独立开发阶段不需要先建立审核员团队；上线初期可以由你本人通过受控管理方式处理极少量举报。只有后续举报量上升、需要委托他人审核时，才启用 P7/P8 并完成角色授予流程。
- 审核队列、审核决定和审核历史接口均已接入服务端持久化限流；限流配置不可用时接口明确失败，不会静默绕过安全策略。
- 核心接口自查补齐了修改密码、签到、工作节奏保存、外观保存、桌宠动作/复活、匿名发声、共鸣、举报、释怀和关怀结算的服务端限流；超过频率都会返回 `rate_limited`，不会只依赖前端按钮状态。

### 已观测到的云端状态

曾使用普通已登录账号打开 P7，页面收到并正确显示“无审核权限”。这至少证明当前 Supabase 环境中的 `moderation-queue` 与角色校验链路可达，普通账号被后端拦截为 `403 forbidden`。

仍需明确验收或部署的部分：

- `moderation-review` 是否已部署并可真实写入审核决定；
- `202608210025_m8_moderation_history.sql` 是否已执行；
- `moderation-history` 是否已部署；
- 专用审核测试账号能否查看队列、处理内容并看到历史摘要。

### 审核角色原则

- 不在前端写审核账号名单；
- 不根据账号名判断权限；
- 不提供普通用户自助成为审核员的页面；
- 角色只由受控 Supabase 管理员在控制台对专用测试账号授予；
- 不在聊天、截图、提交信息、前端文件或日志中暴露真实 Auth User UUID。

---

## 5. 下次开始时的操作

### 5.1 同步和基础检查

在 Codespaces 执行：

```bash
cd /workspaces/XGX
git pull origin main
git log -1 --oneline
npm run check
```

`npm run check` 现在额外包含核心发布检查：确认 M1–M7 的 23 个 Function 同时存在于部署清单、源码和前端适配层，并确认暂缓的 M8 Function 没有混入核心部署。也可以单独运行：

```bash
npm run check:core
```

核心部署后可用一条只读命令确认公开接口连通且没有泄露内部字段：

```bash
npm run smoke:public
```

它只请求 `rants-random?limit=1`，不创建或修改业务数据；未配置公开 URL/anon key 时会明确提示，不会打印 key。

预期最新提交：

```text
555da69 feat: add moderation decision history
```

### 5.2 启动前端

```bash
npm run dev
```

根地址会固定打开 `P1_首页.html`，不用再手输首页文件名。纯前端改动后，停止旧服务并重启，再使用浏览器强制刷新：

```text
Ctrl + C
npm run dev
Ctrl + Shift + R
```

### 5.3 仅在确认需要时部署 Supabase

先检查本次 Git 更新是否包含 `supabase/migrations/` 或 `supabase/functions/`。只有两者变化时，才需要部署。

当前 M8 审核历史需要：

```bash
npm run supabase -- db push
npm run supabase -- functions deploy moderation-history
```

当前主线使用 M1–M7 核心部署命令，不把 M8 审核后台作为上线门禁：

```bash
npm run deploy:core
```

该命令会先运行检查，再执行数据库迁移和 23 个核心 Function 部署；它不会部署 `moderation-queue`、`moderation-review`、`moderation-history` 或 `moderation-role`。仅查看执行顺序时使用：

```bash
npm run deploy:core -- --dry-run
```

为了减少重复输入，也可以使用项目内置的一键部署命令：

```bash
npm run deploy:m8
```

它会按顺序执行项目检查、`db push` 和 3 个 M8 审核 Function 部署。它不读取或写入代码中的密钥；Supabase CLI 使用本地登录态。执行前仍需先完成一次：

```bash
npm run supabase -- login
```

只想检查命令顺序、不连接云端时：

```bash
npm run deploy:m8 -- --dry-run
```

若需完整核对 M8 审核模块，也可明确重新部署：

```bash
npm run supabase -- functions deploy moderation-queue
npm run supabase -- functions deploy moderation-review
```

部署命令不会要求将 `SUPABASE_SERVICE_ROLE_KEY`、数据库密码、`CRON_SECRET`、`RATE_LIMIT_SECRET` 或恢复码 pepper 写入前端或 Git。任何此类敏感内容都不能提交、截图或发送到聊天中。

---

## 6. 当前上线验收清单（M1–M7）

按以下顺序测试，发现问题只修对应模块后再继续。

1. 注册、登录、退出、刷新和失效 Token；错误提示留在当前弹窗。
2. 找回密码、修改密码、新密码登录和恢复码轮换。
3. 签到幂等：重复签到不重复加分，刷新后仍显示已签到。
4. 领导首次创建、动作、倒地、复活、倒地后重新捏；正常外观锁定。
5. 匿名发声每日上限、随机墙、共鸣唯一性和积分流水。
6. 举报自己、重复举报、三人举报自动隐藏；举报错误显示在弹窗内部。
7. 释怀销毁、24 小时奖励、未满足时间门槛时的 0 积分幂等处理。
8. 下班、喝水、久坐设置跨页面同步；重新登录不自动弹设置弹窗。
9. 注销测试账号：错误密码不删，正确密码后 Token 失效、数据级联删除、公开内容消失。
10. 过期内容：随机墙实时排除到期内容；定时任务执行后正文、共鸣和举报关联物理删除；重复任务安全跳过。

M8/P7/P8 的权限和审核验收作为上线后第二阶段，不作为当前独立开发上线的阻塞项。

---

## 7. 待部署与待验收

以下是代码中存在、但云端部署状态或端到端验收尚需逐项确认的能力：

- M3：`pet-action`、`pet-revive`、`pet-get`、`profile-save-appearance`；
- M4：`rants-random`、`rants-create`、`resonance-create`、`rants-mine`、`points-ledger`、`resonance-claim`、`rant-report`；
- M5：`release-complete`、`release-draft`；
- M6：`care-complete`、`work-preferences-save`；
- M7：`account-delete`、`rants-expire`、迁移 `202608210022`、`202608210023`；
- M7：迁移 `202608210026` 为过期清理任务增加并发锁，重复调度安全返回 `skipped`；
- M8：迁移 `202608210024`、`202608210025`，以及 `moderation-queue`、`moderation-review`、`moderation-history`。
- M8：迁移 `202608210027` 与 `moderation-role` 提供受控 moderator 授予/撤销和角色变更审计；admin 角色不可通过业务接口修改。

不要因为前端按钮出现就默认云端函数已部署。接口失败时应优先检查 Supabase Functions 列表、迁移历史和浏览器 Network 响应。

---

## 8. 尚未开始的生产化工作

当前不以正式上线为目标，但后续上线前仍需完成：

- staging 与 production 使用独立 Supabase 项目、域名和密钥；
- 配置 `rants-expire` 每日定时任务，并安全保存 `CRON_SECRET`；
- 生产 CORS 白名单、CSP、网关限流和内容审核服务；
- 监控告警、脱敏日志、备份和恢复演练；
- 受保护的部署身份、GitHub Environments、迁移升级测试和回滚流程；
- 审核员角色的授予/撤销流程、人员变动审计和最小权限管理。

---

## 9. 项目约束与防回归规则

1. 浏览器仅使用 Supabase URL 与 anon/publishable key；禁止任何服务端密钥进入前端、仓库、截图和聊天记录。
2. 弹窗点击遮罩不得默认关闭；表单错误必须显示在当前弹窗内。
3. 正式积分、签到、共鸣、释怀、复活和注销只能以服务端返回结果为准，不能通过前端变量伪造成功。
4. 公开吐槽接口不得返回账号、`owner_id`、举报者、审核字段、恢复码或销毁信息。
5. 所有新增 Edge Function 需同步提供：前端调用、迁移/RPC（如需要）、部署命令、失效会话/重复请求验收项和文档。
6. 每次提交前运行 `npm run check`；完成云端部署后必须强制刷新并进行跨页面验证。
7. 不覆盖 Codespaces 中用户本地的 `scripts/xqx-config.js`；该文件可保存公开连接配置但不应提交敏感值。

---

## 10. 推荐的下一个开发方向

先完成本交接文档第 6 节的 M1–M7 真实验收，再选择以下方向之一：

1. **部署与验收优先**：逐模块确认 Supabase 迁移、Functions 和真实数据流，修复发现的问题。
2. **生产运维优先**：补监控、备份、定时任务、环境隔离和发布流程。
3. **审核运营优先（暂缓）**：只有上线后举报量和运营需求出现时，再启用 M8/P7/P8。

当前最推荐第 1 项：先完成 M1–M7 云端部署和验收。功能代码已经较完整，继续增加运营后台页面的收益低于真实部署、数据联调和回归验收。

### 2026-08-24 本次进展

- 前端优化阶段启动：后端 M1–M7 业务逻辑冻结，V1 以界面体验和交互反馈为主。本轮 P1 首页完成首屏状态文案动态化（未登录、未签到、已签到、已有领导），并优化弹窗窄屏高度、内部滚动、焦点进入/回收和 Esc 关闭行为；保持点击遮罩不关闭规则，不改任何后端接口或业务结算。
- P1 领导卡互动增强：新增当前情绪状态、连续动作连击提示和动作分级台词；戳、拍、喂食、扔东西分别提供更有情绪价值的回应，仍复用现有服务端动作结算和原有动画接口。参考了 MIT 项目 `ykhli/AI-tamago` 的“状态 + 反馈”思路，没有引入其代码或技术栈。
- 动作台词已从首页主体拆到 `scripts/xqx-pet-lines.js`。V1 可直接编辑该文件中的 `poke`、`slap`、`feed`、`throw` 数组扩充词条，刷新页面即可生效；暂不做数据库词条管理，以保持后端冻结。
- 动作台词改为按动作随机抽取，并避免同一动作连续重复上一句；连击数仍用于视觉反馈，不再决定台词顺序。
- 修复 P1 领导倒地后的重复反馈：短时间内同文案 toast 合并为单条并做轻微脉冲，倒地处理增加前端幂等保护，倒地后忽略此前并发动作返回的 `pet_down` 提示；后端状态规则不变。
- P1 四个动作增加实体化前端动画：戳为手指伸入、拍为手掌落下、喂食为苹果飞向人物、扔东西为垃圾桶飞行；动画只影响视觉层，不锁定连续点击，不改后端动作接口。
- 修复关怀提醒完成按钮可能永久卡在“正在记录…”的问题：前端 `callAuthenticated` 支持 AbortSignal，关怀提交增加 12 秒超时；超时后按钮恢复并提示网络超时，同时保留同一 care 类型的 request_id，重试仍走服务端幂等链路。未修改后端 Function 或迁移。
- 优化关怀完成的即时反馈：点击后立即关闭提醒弹窗、显示“已提交，同步中”，对应卡片进入同步态；接口成功后才使用真实 `points_balance` 和结算结果更新积分，失败/超时恢复可重试状态。仍保留 12 秒取消和 request_id 幂等。

- 完成 M1–M7 其余写入接口的服务端限流：`profile-save-appearance`、`pet-action`、`pet-revive`、`rants-create`、`resonance-create`、`resonance-claim`、`rant-report`、`release-complete`、`release-draft`、`care-complete`。
- 限流在身份校验成功后执行，响应统一为 HTTP `429`、`reason=rate_limited` 和 `retry_after_seconds`；幂等键、每日上限和数据库事务仍是业务正确性的最终保障。
- `scripts/check-core-release.mjs` 已把 17 个核心读写接口纳入硬门禁，后续新增核心 Function 若未调用 `consumeRateLimit`，`npm run check` 会失败。
- 本次只修改 Function 入口、检查脚本和文档，不新增迁移；云端更新只需重新部署受影响 Function，不需要再次 `db push`。
- M3 连续发泄体验已调整：取消 `m3_pet_action` 中拍一拍的 5 秒锁定，允许连续点击戳/拍；领导倒地后仍停止动作，数据库档案行锁和积分幂等不变。

本轮还为读取接口增加了宽松保护：随机墙按来源 IP 每分钟 60 次，个人吐槽和积分流水按账号每分钟 60 次，领导状态按账号每分钟 120 次。限流只在服务端生效，正常刷新和跨页面读取不会改变业务数据。

受影响的部署命令：

```bash
npm run supabase -- functions deploy profile-save-appearance
npm run supabase -- functions deploy pet-action
npm run supabase -- functions deploy pet-revive
npm run supabase -- functions deploy rants-create
npm run supabase -- functions deploy resonance-create
npm run supabase -- functions deploy resonance-claim
npm run supabase -- functions deploy rant-report
npm run supabase -- functions deploy release-complete
npm run supabase -- functions deploy release-draft
npm run supabase -- functions deploy care-complete
```

部署前后运行 `npm run check`；云端冒烟仍使用 `npm run smoke:public`，脚本会检查随机墙正常 limit、非法 limit 的响应格式和公开字段脱敏。当前主线仍为 M1–M7 稳定上线基线，M8/P7/P8 暂缓。

本次连续动作调整新增迁移 `202608240028_m3_continuous_pet_actions.sql`，部署时需先执行数据库迁移，再刷新首页前端。

本轮 M4 举报修正新增迁移 `202608240029_m4_report_replay_status.sql`：同一用户重复举报或并发重放时，返回值会反映该内容当前是否已被 3 人举报阈值隐藏。

- 修复 P1 自动恢复已有登录会话时未重启喝水/久坐提醒计时的问题。刷新页面或重新打开页面会读取账号保存的下班、喝水、久坐偏好，并从本次会话恢复时刻重新计时；不会重复弹出设置窗口。
- 本项属于前端状态恢复修复，不需要重新部署 Edge Function；桌面测试副本已同步。验证前使用 `Ctrl + F5`，再等待或临时设置较短的提醒间隔进行验收。
- 修复保存工作节奏设置后仍沿用旧提醒起点的问题。用户修改下班时间、喝水间隔或久坐间隔后，新的设置从保存成功时刻生效并重新开始计时，不重复弹窗。
- 修复喝水/久坐提醒弹窗的完成链路：弹窗“我完成了”现在直接调用现有关怀结算接口，成功后立即同步积分、首页按钮状态和下一次提醒计时，再关闭弹窗；未修改后端代码或接口契约。
- 动作视觉继续做轻量增强：四个 SVG 道具动画整体放慢（约 0.86–1.04 秒），道具保留时间延长至约 1.12 秒，让戳、拍、喂、扔的过程更容易被看清；只改 P1 CSS/JS 视觉层，不改变连续点击和后端结算逻辑。参考方向为 `ykhli/AI-tamago` 的状态反馈思路与 `JIVI07/IN_OUT` 的原生 SVG 微交互，不直接复制代码。
