# 小确闲 · 后端开发文档

> 版本：v0.3（实施基线）
> 日期：2026-08-17  
> 对应产品：`小确闲_v1.1_需求规格说明书.md`（冻结版）  
> 对应前端：P1–P6 原生 HTML 原型  
> 状态：模块化实施中

---

## 1. 目标、边界与实施原则

### 1.1 后端目标

将现有高完成度前端原型升级为具备真实账号、数据同步、匿名公开、服务端积分结算和可审计安全边界的 v1 产品。

首发闭环：

```text
预置桌宠试玩
  → 倒地触发登录
  → 注册赠送免费 DIY + 300 积分
  → 桌宠互动 / 签到 / 关怀 / 吐槽 / 共鸣赚分
  → 积分复活领导
  → 吐槽销毁释怀
```

### 1.2 不在本期后端范围

- 真实支付、会员、积分商城、积分交易或提现；
- 评论、私信、关注、排行榜、热度排序；
- 私密吐槽、吃瓜/热榜；
- 手机号、邮箱绑定及其找回流程；
- 客户端自定义积分值、浏览器直写业务表。

### 1.3 实施基线（当前建议）

采用 **Supabase** 作为 v1 后端基线：Postgres + Auth + Row Level Security（RLS）+ Edge Functions + 定时任务。

选择理由：需求已明确使用“RLS / Edge Function / RPC”边界，且产品需要匿名公开查询、复杂原子积分结算、到期清理和访问控制。若后续改用自建 Node/NestJS 服务，本文的表结构、接口契约、权限与验收规则仍然适用，只需将 Function/RPC 实现迁移到 API 服务。

### 1.4 硬性原则

1. 浏览器只能报告行为，服务端决定状态、积分与奖励资格。
2. 所有积分变更写入不可由客户端伪造的 `point_ledger` 流水。
3. 所有可重试写操作携带幂等键；数据库负责最终唯一性。
4. 公开吐槽接口绝不返回账号、`owner_id`、审核字段、销毁凭证或恢复码相关数据。
5. 密码和恢复码仅保存成熟算法生成的哈希；日志禁止记录明文。
6. RLS 默认拒绝，公开读取仅经受限视图/RPC，关键写操作仅经 Function/RPC。
7. 所有每日上限以服务端配置的自然日与时区为准；客户端时间仅作展示。

---

## 2. 模块安排与依赖

按下列顺序开发。每个模块完成后，先做数据库/API 验收，再接入相应前端页面；未通过验收不进入下一模块。

| 编号 | 模块 | 主要交付 | 前置依赖 | 对应界面 |
|---:|---|---|---|---|
| M0 | 工程与安全基线 | Supabase 工程、迁移、环境变量、RLS 默认策略、错误规范 | 无 | 全局 |
| M1 | 账号与恢复码 | 注册、登录、令牌校验、恢复码轮换、找回密码、注销身份校验 | M0 | P1 / P5 / P4 |
| M2 | 个人档案与积分账本 | `profiles`、初始化权益、积分流水、个人资料读取、签到数据读取 | M1 | P1 / P2 / P4 |
| M3 | 桌宠 | 外观保存、分享预览、状态读取、动作、锁定、倒地、复活/重捏 | M2 | P1 / P2 |
| M4 | 吐槽与共鸣 | 审核、随机墙、发布上限、共鸣、待领取奖励、举报 | M2 | P1 / P4 |
| M5 | 释怀仪式 | 我的吐槽选择、销毁原子操作、24 小时奖励幂等结算 | M4 | P3 / P4 |
| M6 | 签到与关怀 | 签到、连续天数、惊喜保护、喝水/久坐上限、下班时间 | M2 | P1 / P4 |
| M7 | 运维与上线验收 | 15 天过期清理、限流、监控、备份、全链路安全与压测 | M1–M6 | 全局 |

### 2.1 当前实施状态

M0、M1 已完成；M2 的个人资料、积分余额/流水基础和 M6 的签到服务端结算已接入开发环境；M3 已完成服务端动作、倒地、复活、独立状态读取和重新捏的基础；M4 的吐槽与共鸣基础已完成；M5 已接入匿名发声销毁结算；M7 已开始补齐 15 天到期生命周期。后续所有模块均遵循“迁移/RLS → Edge Function → 前端适配 → 云端验收”的顺序，不再把正式账号功能停留在浏览器本地模拟状态。

---

## 3. 工程目录建议

若采用 Supabase CLI，仓库新增如下目录（迁移文件按时间戳命名）：

```text
supabase/
  config.toml
  migrations/
    <timestamp>_m0_extensions_and_types.sql
    <timestamp>_m1_auth_and_profile_schema.sql
    ...
  functions/
    auth-register/
    auth-reset-password/
    pet-act/
    pet-revive/
    rants-create/
    resonance-create/
    release-destroy/
    checkin-sign/
    care-complete/
  seed.sql                         # 仅本地开发/测试数据
  tests/
    sql/
    functions/
```

前端后续新增统一适配层，而不是在 HTML 中散落请求：

```text
assets/js/
  api-client.js
  auth-store.js
  profile-store.js
  error-messages.js
```

---

## 4. 身份与账号设计（M1）

### 4.1 身份模型

- 对外字段名称统一为“账号”；趣味文案可称“牛马编号”或“摸鱼代号”。
- 账号规则：4–20 位，`a-z` / `A-Z` / `0-9` / `_`；全局唯一；存储时保留展示值并维护标准化唯一值。
- 密码：8–32 位，字母、数字、符号任意两类；不要求固定周期修改。
- 不绑定手机、邮箱、昵称或头像。
- 账号不出现在任何公开查询、吐槽卡、公共响应和审计日志中。

### 4.2 推荐实现

Supabase Auth 原生以邮箱/手机号为核心。为满足“账号 + 密码、无邮箱”的产品要求，使用受控的 Edge Function 注册/登录入口，并建立 `account_credentials` 映射表：

- 账号标准化值仅存在受限表；
- 服务端使用不可逆、内部派生的登录标识创建 Auth 用户；
- 前端永远只传账号和密码；
- 派生标识、恢复码、密码不返回前端日志；
- Service Role Key 仅在 Edge Function 环境中使用，绝不进入前端。

> 实现开始前必须确认 Supabase Auth 当前版本支持的管理员创建、密码校验与匿名账号策略；若存在限制，则用独立认证服务或自建认证表替代，但不得降低密码哈希与会话安全要求。

### 4.3 恢复码

- 注册成功生成一次高强度恢复码，例如 `XQX-xxxx-xxxx-xxxx-xxxx`；明文只在本次响应中出现。
- 数据库存储恢复码哈希、生成时间、使用/作废时间；不可回读明文。
- 找回密码提交 `account + recovery_code + new_password`；成功后原恢复码失效，并生成新的恢复码。
- 密码和恢复码同时丢失时拒绝找回，管理员不得绕过认证接管账号。

### 4.4 M1 接口契约

#### `POST /functions/v1/auth-register`

请求：

```json
{
  "account": "workhorse_01",
  "password": "example-password",
  "request_id": "uuid"
}
```

成功响应：

```json
{
  "ok": true,
  "access_token": "...",
  "refresh_token": "...",
  "profile": { "points_balance": 300, "free_diy_available": true },
  "recovery_code": "XQX-..."
}
```

约束：账号唯一；`request_id` 幂等；注册成功原子初始化 M2 所需档案与欢迎积分；恢复码只在这一响应出现一次。

#### `POST /functions/v1/auth-login`

请求：`{ "account": "workhorse_01", "password": "..." }`

响应：`{ "ok": true, "access_token": "...", "refresh_token": "...", "profile": { ... } }`

失败统一返回模糊的 `invalid_credentials`，不泄露账号是否存在。接口按账号、IP、设备维度限流。

#### `POST /functions/v1/auth-reset-password`

请求：

```json
{
  "account": "workhorse_01",
  "recovery_code": "XQX-...",
  "new_password": "...",
  "request_id": "uuid"
}
```

成功响应：`{ "ok": true, "recovery_code": "XQX-..." }`。

失败统一为 `invalid_recovery` 或 `rate_limited`；不得披露账号、恢复码哪一项错误。

#### `POST /functions/v1/account-delete`

需要登录态并再次验证当前密码；M7 实现为立即且不可恢复的硬删除。请求为 `{ "current_password": "...", "confirmation": "注销", "request_id": "uuid" }`。服务端先校验 JWT、密码和 10 分钟内 3 次的操作限流，再删除 Auth 用户；所有以该用户为外键根的数据（档案、积分流水、恢复码、桌宠保存、签到、关怀、匿名吐槽、共鸣、举报关联）由 `on delete cascade` 同步清除。公开吐槽在删除事务完成后立刻从随机墙消失。

---

## 5. 数据模型

### 5.1 公共枚举

```text
pet_action      = poke | slap | feed | throw
point_event     = welcome | pet_action | rant_publish | resonance_give |
                  resonance_claim | release_complete | checkin |
                  care_water | care_sit | pet_revive
rant_status     = pending | published | destroyed | expired | rejected
review_status   = pending | approved | rejected | hidden
care_type       = water | sit
```

### 5.2 核心表

#### `profiles`

| 字段 | 类型 | 说明 |
|---|---|---|
| `user_id` | uuid PK | 对应 Auth 用户 |
| `points_balance` | integer | 当前可用积分，非负 |
| `free_diy_available` | boolean | 注册权益；首次使用后为 false |
| `appearance` | jsonb | 含 `version` 的 8 维桌宠外观 |
| `pet_status` | jsonb | endure/irritate/fat/dirty/down/locked_until |
| `off_hour`, `off_min` | smallint | 下班时间，默认 18:00 |
| `created_at`, `updated_at` | timestamptz | 审计时间 |

#### `point_ledger`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid PK | 流水 ID |
| `user_id` | uuid | 所有者 |
| `event_type` | enum | 积分事件类型 |
| `delta` | integer | 正负积分变化 |
| `idempotency_key` | text | 调用方/服务端唯一事件键 |
| `reference_type`, `reference_id` | text/uuid | 关联吐槽、共鸣、签到等 |
| `metadata` | jsonb | 安全的附加上下文 |
| `created_at` | timestamptz | 发生时间 |

唯一约束：`(user_id, idempotency_key)`；对服务端生成的全局事件键使用全局唯一索引。

#### `rants`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid PK | 吐槽 ID |
| `owner_id` | uuid | 仅服务端/本人可见 |
| `content` | text | 纯文本，1–50 用户可见字符 |
| `status` | enum | 生命周期状态 |
| `review_status` | enum | 审核结果 |
| `votes` | integer | 有效共鸣总数 |
| `created_at`, `expires_at`, `destroyed_at` | timestamptz | 生命周期 |
| `release_rewarded_at` | timestamptz nullable | 防止释怀重复奖励 |

#### `resonances`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid PK | 共鸣 ID |
| `rant_id` | uuid | 关联吐槽 |
| `actor_id` | uuid | 共鸣者，仅内部可见 |
| `owner_id` | uuid | 作者，仅内部可见 |
| `clicker_rewarded` | boolean | 当日是否给点击者奖励 |
| `owner_claimed_at` | timestamptz nullable | 作者是否领取 |
| `created_at` | timestamptz | 创建时间 |

唯一约束：`(rant_id, actor_id)`；检查约束：`actor_id <> owner_id`。

#### `checkins` / `care_completions`

分别记录服务端自然日签到和关怀完成事件。使用 `(user_id, business_date)`、`(user_id, business_date, type, sequence)` 等唯一键约束，避免刷新或并发重放得分。

#### `account_recovery_codes`

存储 `user_id`、`code_hash`、`created_at`、`revoked_at`、`used_at`。仅 Edge Function 通过 Service Role 访问。

#### `rant_reports`

记录公开吐槽举报；包含 `rant_id`、举报者、原因和创建时间。公开端不返回举报数据；达到阈值后的临时处置通过关联吐槽的 `review_status='hidden'` 表示，后续管理员体系再补充人工处置字段。

### 5.3 公共安全视图

建立 `public_rants_random` 受限视图/RPC，只能输出：

```json
{ "id": "uuid", "content": "...", "votes": 12, "created_at": "...", "expires_at": "..." }
```

禁止使用 `select *`，禁止暴露 `owner_id`、审核状态、销毁信息、内部评分或任何可关联用户身份的字段。

---

## 6. 统一积分规则与原子结算

| 行为 | 默认积分 | 服务端限制 |
|---|---:|---|
| 首次注册欢迎 | +300 | 每账号一次 |
| 首次注册免费 DIY | 免费 | 每账号一次 |
| 桌宠有效动作 | +10 | 由远端配置控制；倒地/锁定不奖励 |
| 公开吐槽审核入墙 | +50 | 每日 3 条 |
| 给他人有效共鸣 | +10 | 每天前 10 次 |
| 收到有效共鸣 | +10 | 待作者一次性领取 |
| 完整释怀 | +100 | 满 24 小时、看完、每条一次 |
| 每日签到 | +100 + 惊喜 | 每日一次 |
| 喝水 | +20 | 每日最多 6 次奖励 |
| 久坐 | +30 | 每日最多 4 次奖励 |
| 复活原领导 | -500 | 必须积分足够且处于倒地状态 |

所有规则读取服务端 `app_config`（或等价配置表）。前端可展示当前配置，但不得传入积分数值。涉及状态修改和积分变更的操作必须处于同一数据库事务中：

```text
验证身份 / 所有权 / 资格
  → 检查幂等键与每日上限
  → 修改业务状态
  → 写入 point_ledger
  → 更新 profiles.points_balance
  → 返回最终状态
```

---

## 7. 页面接口地图

### P1 首页

保留现有接口文档定义并实现：

- `POST /functions/v1/pet-get`
- `POST /rpc/pet.act`
- `POST /rpc/pet.revive`
- `GET /rpc/rants.random?limit=10`
- `POST /rpc/rants.create`
- `POST /rpc/resonance.create`
- `GET /rpc/checkin.status`
- `POST /rpc/checkin.sign`
- `POST /rpc/care.complete`
- `PUT /rpc/profile.offtime`

### P2 捏脸 DIY

新增：

- `POST /functions/v1/pet-get`：返回本人 8 维外观、桌宠状态、免费 DIY 权益与 `will_revive_on_save`。
- `POST /functions/v1/profile-save-appearance`：请求 `{ "appearance": {...}, "request_id": "uuid" }`；验证字段范围与登录身份。仅首次创建或旧领导倒地时允许保存；后者会由服务端同时恢复满血。
- 分享 URL 不调接口，只编码外观；接收方先本地预览，确认后调用自己的 `PUT /rpc/pet.appearance`。

### P3 释怀仪式

- `GET /rpc/rants.mine?eligible_for_release=true`：只返回本人未销毁、未过期的候选吐槽及是否满 24 小时。
- `POST /rpc/release.begin`：`{ "rant_id": "uuid", "request_id": "uuid" }`；在事务中验证所有权并先将吐槽标记为 destroyed，返回一次性演出凭证或安全操作 ID。
- `POST /rpc/release.complete`：`{ "release_id": "uuid", "watched_complete": true }`；服务端仅在已满 24 小时且未发过奖励时加分。动画跳过则由前端以 `watched_complete:false` 调用或不调用该接口。

### P4 个人中心

- `GET /rpc/profile.me`
- `GET /rpc/points.ledger?cursor=&limit=20`
- `GET /rpc/rants.mine?cursor=&limit=20`
- `POST /rpc/resonance.claim`：一次性领取本人所有待领取共鸣奖励。
- `GET /rpc/checkin.history?month=YYYY-MM`
- `PUT /rpc/account.password`
- `POST /rpc/account.recovery-code.rotate`：需当前密码验证，返回一次性明文恢复码。
- `POST /rpc/account.delete`：二次密码验证 + 冷静期/最终确认策略由 M7 定稿。

### P5 找回密码

使用 M1 的 `auth-reset-password`；前端不得预判恢复码是否有效。

### P6 规则与隐私

不需要业务接口。上线前将规则文案中的积分值与服务端 `app_config` 做人工版本校验。

---

## 8. 安全、审核、限流与数据生命周期

### 8.1 内容审核

发布请求按以下顺序处理：

1. 服务端规范化空白、检查 1–50 个用户可见字符与纯文本；
2. 检测手机号、邮箱、微信号、身份证号、详细地址、工号等可识别信息；
3. 检测禁止内容：违法、威胁、隐私、歧视、色情、诈骗、严重伤害性内容；
4. 通过时写入 `published/approved`，否则不入公开墙；
5. 原因返回使用安全、概括性代码，避免泄露审核规则细节。

客户端预检只改善体验，不具有安全权威性。

### 8.2 限流建议

| 接口 | 建议限流策略 |
|---|---|
| 注册 / 登录 / 找回密码 | 账号 + IP + 设备指纹分层；指数退避 |
| 发布吐槽 | 用户每日 3 条硬限制 + 短窗请求限流 |
| 共鸣 | 用户短窗限流 + 数据库唯一约束 |
| 桌宠动作 | 按用户、轮次和锁定期校验；可批量结算 |
| 积分和销毁 | 幂等键、事务、审计日志、异常告警 |

### 8.3 生命周期

- 公开吐槽创建时写入 `expires_at = created_at + 15 days`。
- 每次公开查询实时过滤 `published + approved + expires_at > now()`，不得只依赖定时任务。
- 定时任务删除到期公开吐槽原文及其共鸣、举报关联；任务失败不应导致公开查询泄露过期内容，因为公开查询仍实时过滤 `expires_at`。
- 用户注销时立即销毁该用户公开吐槽，按数据保留策略删除/匿名化个人数据。

---

## 9. 模块验收清单

### M0：工程与安全基线

- [ ] 迁移可在全新本地数据库重复执行；
- [ ] 前端环境变量只有匿名公钥与公开 URL，无 Service Role Key；
- [ ] 业务表开启 RLS，默认无直接写权限；
- [ ] 统一错误格式：`{ ok:false, reason:"...", request_id:"..." }`；
- [ ] 开发、测试、生产环境与密钥隔离。

### M1：账号与恢复码

- [ ] 合法账号可注册，重复账号不可注册；
- [ ] 注册原子生成档案、300 欢迎积分、免费 DIY 权益和恢复码哈希；
- [ ] 密码、恢复码不出现在数据库明文、错误响应或日志；
- [ ] 登录失败不枚举账号；
- [ ] 使用正确恢复码能重置密码并轮换恢复码；
- [ ] 错误/过期/已使用恢复码失败；
- [ ] 注册、登录、找回均可限流且返回安全错误。

#### 当前实现（2026-08-18，联调中）

- 迁移：`202608170002_m1_auth_and_profile_schema.sql`，以及恢复码保留锁修复迁移 `202608180003_m1_recovery_reservation_fix.sql`；
- 数据表：账号映射、最小个人档案、积分流水、恢复码、恢复保留锁、注册请求记录、认证限流；
- 内部 RPC：注册初始化、内部登录标识查询、恢复码保留/轮换、数据库持久化限流；
- Edge Functions：`auth-register`、`auth-login`、`auth-reset-password`；
- 恢复码采用 Edge Function 中 PBKDF2-SHA-256 + 独立 pepper 的哈希，不保存明文；
- 已完成：开发环境的密钥设置、数据库迁移、注册/登录函数部署和注册联调。
- 联调修复：恢复码重置的保留锁函数曾因 `user_id` 返回字段与表字段同名导致查询歧义；由 `202608180003` 使用表别名修正。
- 已验收（2026-08-18）：注册返回 201、初始 300 积分和免费 DIY 权益；恢复码重置返回 200 并轮换恢复码；使用新密码登录返回 200。
- 后续补测：重复注册、错误/过期/已使用恢复码、认证频率限制，以及 P1/P4/P5 前端真实接口联调。

### M2–M7

各模块实现前，先将其接口、迁移、RLS、幂等条件、边界测试补充到本文件对应章节；通过单元测试、集成测试和前端联调后才标记完成。

### 当前实现：M2 个人档案与 M6 签到基础

- `profile-me`：登录态读取账号展示名、积分、DIY 权益、外观、桌宠状态、真实连续签到天数和签到日期；账号映射仍只通过 Service Role 读取。
- `202608180006_m2_checkin_and_diy_entitlement.sql`：签到唯一键、服务端连续签到计算、惊喜与里程碑积分、`point_ledger` 原子流水，以及首次保存 DIY 后消耗免费权益。
- `checkin-sign`：登录态签到接口；重复请求返回 `already_signed`，不重复增加积分。
- 当前状态：开发环境已部署并正在联调；签到和个人资料接口必须在 Codespaces 中部署后，P1/P4 才能读取真实结果。

### 当前实现：M6 工作节奏偏好（开发完成，待部署）

- `202608210017_m6_work_preferences.sql` 为 `profiles` 增加 `water_interval`、`sit_interval`，并限制为 30/45/60/90 分钟；已有的 `off_hour`、`off_min` 继续作为下班时间字段。
- `work-preferences-save` 仅接受登录态，校验小时、分钟和两个提醒间隔后更新当前账号；浏览器没有直接更新 `profiles` 的权限。
- `profile-me` 返回四个偏好字段。P1 登录恢复时以云端设置覆盖本地缓存，保存失败时停留在当前设置弹窗并提示错误，不伪造同步成功。

### 当前实现：M3 桌宠互动（开发中）

- 迁移：`202608180007_m3_pet_action.sql`。
- RPC：`m3_pet_action(user_id, pet_action, request_id)`。
- Edge Function：`pet-action`。
- Edge Function：`pet-revive`，登录态下复活倒地领导，服务端按 `points.pet_revive` 扣分并恢复满血状态；重复请求幂等。
- 服务端负责校验倒地、锁定、动作枚举、状态变化、积分配置、幂等键和最终余额；客户端只发送动作和请求 ID。
- `pet_status` 变化与 `point_ledger` 写入处于同一数据库事务；重复请求返回第一次结算结果，不重复加分。
- `slap` 锁定 5 秒；`feed` 达到胖度上限返回 `burst`；`throw` 达到脏度上限返回 `faint`；承受值归零后标记 `down`。
- 倒地状态不会因为登录、刷新或切换页面自动复活；P1 必须显示“复活 / 重新捏领导”入口。复活成功后才由服务端返回的新状态解除 `down`。
- 重新保存新的领导外观时，`m1_save_appearance` 同一事务会把桌宠重置为满血并解除 `down`，避免“重新捏完仍倒地”的状态残留。
- `202608210016_m3_pet_read_and_reface.sql` 新增 `pet_appearance_saves` 请求幂等表和 `m3_save_appearance`：首次保存消耗免费 DIY；旧领导倒地时可保存新外观，并以同一事务恢复满血状态。正常已保存领导的 P2 入口由前端和服务端读取状态共同限制。重复 `request_id` 返回原结果，不重复修改状态。
- `pet-get` 提供独立的登录态桌宠读取和 `will_revive_on_save` 提示；P2 的最终外观与桌宠状态必须使用服务器响应，不能通过本地变量伪造复活。
- P1 的 2D 人物预览同步提供戳动、拍扁、喂食弹跳和扔东西甩动动画；不能只调用当前不可见的 3D Canvas 动画。

### 当前实现：M4 吐槽与共鸣（开发中）

- 迁移：`202608180010_m4_rants_and_resonance.sql`。
- 修复迁移：`202608180012_m4_resonance_correctness_fix.sql`、`202608180014_m4_rant_business_day_fix.sql`；所有每日上限统一按 `Asia/Shanghai` 业务日计算，共鸣重放始终返回当前余额和奖励次数。
- 数据表：`rants`、`resonances`；浏览器无直接写权限，公开随机墙只经 Edge Function 输出安全字段。
- Edge Functions：`rants-random`（匿名只读）、`rants-create`（登录态发布）、`resonance-create`（登录态共鸣）。
- 发布采用 `request_id` 幂等键、服务端每日上限、积分流水和余额原子更新；开发环境默认进入 `published/approved`，生产接入审核器前不得扩大公开范围。
- 共鸣禁止给自己点赞、同一用户对同一吐槽只能一次；点击者每天前 10 次可获得配置积分，作者可通过 `resonance-claim` 领取尚未领取的共鸣奖励。
- `rants-mine`、`points-ledger`、`resonance-claim` 为 P4 提供真实“我的吐槽”、积分流水和领取奖励；P1 的随机墙、发布和共鸣不再使用浏览器示例数据。
- `202608210021_m4_rant_reports.sql` 与 `rant-report`：登录用户可匿名举报公开吐槽；同一用户对同一条仅一次，不能举报自己的内容。累计 3 位不同用户举报时仅隐藏公开展示，保留原始记录供后续受控人工核查。

### 当前实现：M5 匿名发声释怀（开发中）

- 迁移：`202608180015_m5_release_rant.sql`；RPC `m5_release_rant` 在事务中锁定本人吐槽，标记 `destroyed`，并以 `release_complete:<request_id>` 防止重复销毁或重复加分。
- Edge Function：`release-complete`，仅接受登录态和本人 `rant_id`；发布时间达到配置的 `minimum_age_hours` 才发放 `points.release_complete`，不满足时仍完成销毁但积分为 0。
- 产品流程调整：释怀是匿名发声的后续操作，不再作为独立写内容的模块。P4「我的吐槽」对每条 `published` 内容提供“选择释怀方式”；进入 P3 时携带 `rant_id` 并预选该内容。P3 仍允许从本人未销毁内容中重新选择，但不再手写新内容。
- 发布入口在 Edge Function 中拒绝明显手机号、邮箱和身份证号；这只是最低层预筛，正式上线前仍需接入更完整的内容审核策略。
- `202608210018_m5_release_draft_and_zero_ledger.sql` 修复无奖励释怀的流水约束：`release_complete` 允许记录 0 积分，以便未满 24 小时的销毁仍可正确完成且保持幂等。
- `release-draft` 与 `m5_release_draft` 保留为历史兼容接口，但当前前端不再调用；后续版本评审后再决定是否移除，避免已有部署版本在升级时中断。

### 当前实现：M7 账号注销与数据销毁（代码完成，待云端部署）

- 迁移：`202608210022_m7_account_deletion.sql`，明确 `rants`、`resonances`、`rant_reports` 对 Auth 用户的级联删除关系；浏览器仍无业务表直接写权限。
- Edge Function：`account-delete`。仅接受带 Bearer JWT 的 `POST`，并强制校验当前密码、确认词 `注销`、UUID 请求键和每账号/IP 10 分钟 3 次的操作限流。
- 成功后调用 Supabase Auth 的硬删除；以该用户为根的档案、积分流水、恢复码、签到、关怀、外观保存、吐槽、共鸣和举报关联数据自动删除。函数只在 Auth 删除成功后返回成功，前端才会清除本地会话并跳回首页。
- 云端验收必须使用专门的测试账号：错误密码不删除、缺少确认词不删除、正确密码删除后旧 Token 失效、同账号无法再登录、其公开吐槽不再出现在随机墙。

### 当前实现：M7 提交前契约检查（开发完成）

- `npm run check` 同时执行前端静态基线与后端契约检查；它不访问 Supabase、不需要密钥，适合每次提交前在 Codespaces 运行。
- 后端检查确认所有迁移具备事务边界（历史单语句迁移 `202608210020_m3_lock_normal_appearance.sql` 除外）、关键 Edge Function 有可执行入口，以及账户注销、到期吐槽物理清理两条高风险链路没有断开。
- `.github/workflows/ci.yml` 会在推送 `main` 或创建 Pull Request 时自动执行同一套 `npm run check`。该工作流只读代码，绝不保存或使用 Supabase 密钥，也不执行数据库迁移或函数部署。

### 当前实现：M8 举报人工审核（代码完成，待云端部署）

- `202608210024_m8_moderation_workflow.sql` 新增 `user_roles` 与 `rant_moderation_actions`；两个表均开启 RLS，并撤销了普通用户的所有权限。举报人、发布者账号和审核备注不会出现在公开随机墙或普通用户接口中。
- `moderation-queue` 仅向拥有 `moderator` 或 `admin` 角色的登录态返回被举报内容、举报数量和举报原因集合；不返回 `owner_id`、举报者 ID 或账号映射。
- `moderation-review` 仅允许受权审核者将公开内容决定为 `approved`、`hidden` 或 `rejected`，审核操作使用 `request_id` 幂等并写入审计记录。三人举报的自动隐藏仍然保留；人工审核可恢复误隐藏内容。
- 前端配套页面为独立的 `P7_审核后台.html`，不会出现在普通用户导航中；它只显示审核所需的匿名内容和举报汇总，加载时仍必须通过 Function 的服务端角色校验。
- P1 登录成功后通过 `moderation-queue` 探测当前会话的服务端审核权限：审核账号自动跳转 P7，普通账号保持原首页流程。探测失败默认进入首页，不能影响用户登录；浏览器不存储审核账号名单或权限判断结果。
- 首个审核员不得由前端自助创建。部署后只能在 Supabase SQL Editor 中由受控管理员对已确认的 Auth 用户 UUID 执行角色授予；该操作不做成项目迁移、前端功能或自动化部署步骤，且不得在截图、日志和提交信息中泄露真实用户 UUID。

### 防回归规则（本次问题固化）

1. 弹窗遮罩点击不得默认关闭表单；关闭必须来自明确的关闭、取消或完成操作。错误信息优先显示在当前弹窗内部。
2. 登录态页面必须调用 `XQXAuth.profile()` 完成令牌刷新后再判断会话，禁止只检查原始 `localStorage` Token。
3. 账号名、积分、签到、DIY 权益、桌宠状态等正式数据不得使用 HTML 写死值；页面初始值只能是“读取中/--”，最终值必须来自云端。
4. 积分变化不得只修改前端变量；每次正式奖励必须有服务端事务和 `point_ledger` 唯一幂等键。
5. 每个新增 Edge Function 必须同时提交入口文件、前端调用适配、部署命令和至少一个重复请求/失效会话验收项。
6. 云端部署后必须做一次强制刷新和跨页面验证，避免把旧缓存误判为新代码。
7. 倒地提示只能在一次新的倒地状态中自动出现一次；用户主动打开复活入口不受此限制。
8. 登录、刷新或云端恢复 `pet_status.down=true` 只恢复历史倒地状态，不得把它当作新的倒地事件自动打开复活弹窗；复活请求失败也只显示当前弹窗内的错误，不得重复打开弹窗。
9. 签到后的下班节奏和关怀提醒属于前端时间体验，不能伪造积分；关怀积分仍必须通过 `care-complete` 服务端结算。
10. 登录视觉素材与认证逻辑分离；更换 GIF/WebP/PNG 只能修改公开视觉配置，不得把敏感配置或认证流程写入素材。
11. 已保存领导外观必须由 `profile-me.appearance` 驱动首页入口：正常领导时首页只进入“我的领导/开始暴揍领导”展示区；P2 仅允许首次创建或倒地后的重新捏领导。P1 导航、页脚和 P2 页面加载本身都必须执行该限制，不能依赖单一入口或首次免费 DIY 标记。
12. P2 的外观选项和 2D 预览不得依赖外部 3D CDN；Three.js/WebGL 失败只能降级视觉效果，不能使脸型选择或最终保存流程失效。
13. 工作节奏设置只能由用户主动打开，登录、刷新或会话恢复不得自动弹窗；登录成功后必须读取云端偏好，并以本次登录时刻重新开始喝水与久坐的提醒倒计时。
14. 每次提交后端变更前执行 `npm run check`；它会检查迁移事务边界、关键 Edge Function 入口、账户注销二次确认链路和到期吐槽物理清理链路。历史迁移 `202608210020_m3_lock_normal_appearance.sql` 是已知的单语句例外，后续迁移不得仿效。

---

## 10. 当前待办与变更记录

生产拓扑、服务器安全、备份、监控、发布和回滚规则见：
`小确闲_生产环境部署规划.md`。

### 当前待办

1. 完成 M3 `pet-action`、`pet-revive`、`pet-get`、`profile-save-appearance` 的云端部署和 P1/P2 验收；
2. 验收 M3 免费首次保存、健康状态调整外观、倒地重新捏和相同 `request_id` 重放，确认 P2 以服务端状态更新；
3. 部署 M4 `rants-random`、`rants-create`、`resonance-create`、`rants-mine`、`points-ledger`、`resonance-claim` 并做 P1/P4 真实数据联调；
4. 部署 M5 `release-complete`、`release-draft` 和 `202608210018_m5_release_draft_and_zero_ledger.sql`，验收本人校验、24 小时门槛、0 积分销毁、私密草稿、重复请求和已销毁吐槽重放；
5. 部署 M6 `care-complete`、`work-preferences-save` 和 `202608210017_m6_work_preferences.sql`，验收重复领取、每日上限、跨浏览器偏好同步和失效会话；
6. 部署 `202608210020_m3_lock_normal_appearance.sql`，验收正常已保存领导调用 `profile-save-appearance` 返回 `appearance_locked`，倒地重捏和首次创建仍可成功；
7. 部署 `202608210021_m4_rant_reports.sql` 和 `rant-report`，验收同人重复举报、举报本人、3 位不同用户自动隐藏和匿名公开字段；
8. 为每个已完成模块补充重复请求、过期 Token、刷新页面和跨页面验收。
9. 部署 M7 `202608210019_m7_rant_expiration.sql` 与 `rants-expire`，在 Supabase 定时任务中每天调用一次 `POST /functions/v1/rants-expire`；请求使用仅存于定时任务密钥库的 `CRON_SECRET`，不得暴露给浏览器。
10. 部署 M7 `202608210022_m7_account_deletion.sql` 与 `account-delete`，仅使用专门测试账号验证错误密码、删除成功、Token 失效和公开吐槽消失；不得用主账号做不可逆测试。
11. 部署 `202608210023_m7_expire_rants_purge.sql`；确认定时任务执行后，已到期的 `published/expired` 吐槽会被物理删除，并由外键删除共鸣和举报关联。公开墙仍必须在任务未执行时实时过滤到期内容。
12. 在 GitHub 的 Actions 页面确认 `Verify project / Static and backend contracts` 通过；它只能证明仓库结构和高风险调用链完整，不能替代 Supabase 云端部署与真实账号验收。
13. 部署 `202608210024_m8_moderation_workflow.sql`、`moderation-queue`、`moderation-review`；由受控 Supabase 管理员手工授予一个专用测试审核账号角色，并验收普通用户返回 `403 forbidden`、审核员可读取去身份化举报队列、重复 `request_id` 不重复写入审核记录。

### 变更记录

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-08-17 | v0.1 | 建立模块顺序、架构基线、核心数据模型、P1–P6 接口地图与 M0/M1 验收标准 |
| 2026-08-18 | v0.2 | M1 已部署至 xqx-dev 并通过注册、恢复码重置、新密码登录端到端验收；补充 `202608180003` 修复恢复码保留锁字段歧义。 |
| 2026-08-18 | v0.3 | 补充 `profile-me`、签到服务端结算、DIY 权益消耗、弹窗/登录态防回归规则；开始 M3 桌宠互动原子结算。 |
| 2026-08-18 | v0.4 | 完成 M3 动作、倒地、复活与重新捏状态恢复基础；开始 M4 匿名吐槽、随机墙和共鸣服务端实现。 |
| 2026-08-18 | v0.5 | 自查修复：登录态恢复延后至 DOM 就绪；签到惊喜使用单次随机值并统一上海业务日；P1/P4 清除积分、吐槽与共鸣的本地模拟结算，补齐个人流水、作者领取共鸣和关怀结算接口。 |
| 2026-08-18 | v0.6 | P1 移除开发状态切换器；倒地提示去重；签到成功后替换为下班航线；下班设置新增喝水/久坐频率，并按工作时段执行本地弹窗提醒。 |
| 2026-08-18 | v0.7 | 登录/注册弹窗改为圆角双栏布局，加入可替换的下班主题动态视觉区，移动端自动折叠。 |
| 2026-08-18 | v0.9 | 登录后按服务端 `signed_today` 立即切换下班航线；开始 M5 释怀仪式服务端销毁与 24 小时奖励幂等结算。 |
| 2026-08-18 | v0.8 | 修复历史倒地状态在刷新、登录恢复或复活失败时误触发复活弹窗；仅新的倒地事件自动提醒一次。 |
| 2026-08-21 | v1.0 | 补齐 M3 独立桌宠读取与重新捏服务端校验：新增外观保存幂等表；首次使用免费 DIY，外观可持续调整；倒地后保存会由服务器重新捏并重置满血。 |
| 2026-08-21 | v1.1 | 修复 P1/P2 前端状态衔接：保存外观后首页主入口改为查看我的领导；首次成功登录引导设置下班、喝水和久坐频率；P2 可见捏脸选项从 3D 渲染依赖中解耦。 |
| 2026-08-21 | v1.2 | M6 工作节奏偏好服务端化：下班、喝水和久坐设置随账号同步；首页已保存领导的主入口调整为“开始暴揍领导”。 |
| 2026-08-21 | v1.3 | 补齐 M5 释怀主流程：修复 P3 输入后下一步被禁用；新增私密手写释怀与 0 积分销毁幂等结算，手写内容不进入吐槽墙。 |
| 2026-08-21 | v1.4 | 调整匿名发声后的释怀流程；隐藏已释怀内容；工作节奏设置改为用户主动打开，登录后按云端偏好重启提醒倒计时；开始 M7 过期内容状态归档。 |
| 2026-08-21 | v1.5 | 收紧 P2 业务入口：正常已保存领导禁止进入 DIY，只有首次创建或倒地重捏可用；顶部导航移除下班倒计时，工作节奏仍由签到后的航线面板承载。 |
| 2026-08-21 | v1.6 | 新增 M3 外观锁迁移：服务端拒绝正常已保存领导的外观保存请求，前端跳转守卫与数据库规则保持一致。 |
| 2026-08-21 | v1.7 | 补齐 M4 举报闭环：匿名举报、同人去重、举报本人拦截和 3 人阈值自动隐藏公开内容。 |
| 2026-08-21 | v1.8 | 补齐 M7 提交前质量门禁：新增 GitHub Actions 自动运行静态与后端契约检查；明确检查不含云端部署，保留逐项 Supabase 验收。 |
| 2026-08-21 | v1.9 | 新增 M8 受控举报审核后端：角色隔离、去身份化审核队列、审核审计与幂等决定；普通用户无举报数据读取权限。 |
