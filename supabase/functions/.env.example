# 仅用于本地/临时 Function 调试的变量名示例；不要填写真实值后提交。
# 托管 Supabase Edge Functions 自动提供 SUPABASE_URL、SUPABASE_ANON_KEY、SUPABASE_SERVICE_ROLE_KEY。

# 生产和开发环境均必须分别生成的高熵随机值。
RECOVERY_CODE_PEPPER=
RATE_LIMIT_SECRET=

# 逗号分隔的前端来源。xqx-dev 暂可留空以支持临时预览；上线前必须显式配置 HTTPS 域名。
XQX_ALLOWED_ORIGINS=
